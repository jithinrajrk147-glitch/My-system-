name: Build Android APK
on: [workflow_dispatch]
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Create package.json
        run: |
          cat > package.json << 'EOF'
          {
            "name": "tg-collector",
            "version": "1.0.0",
            "dependencies": {
              "@capacitor/android": "^6.1.2",
              "@capacitor/core": "^6.1.2",
              "@capacitor/local-notifications": "^6.1.2"
            },
            "devDependencies": {
              "@capacitor/cli": "^6.1.2"
            }
          }
          EOF

      - name: Create capacitor.config.json
        run: |
          cat > capacitor.config.json << 'EOF'
          {
            "appId": "com.anxroall.collector",
            "appName": "TG Collector",
            "webDir": "www",
            "server": {
              "url": "https://anxroall-a.github.io/Websitesdatabase/index.html",
              "cleartext": true
            },
            "android": {
              "allowMixedContent": true
            },
            "plugins": {
              "LocalNotifications": {
                "smallIcon": "ic_stat_icon_default",
                "iconColor": "#00ff99",
                "sound": "default"
              }
            }
          }
          EOF

      - name: Install dependencies
        run: npm install

      - name: Add Android platform
        run: npx cap add android

      - name: Prepare web assets
        run: |
          mkdir www
          echo '<!DOCTYPE html><html><head><meta charset="UTF-8"><title>TG Collector</title><script>location.href="https://anxroall-a.github.io/Websitesdatabase/index.html"</script></head><body>Loading...</body></html>' > www/index.html

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Add permissions and receivers
        run: |
          sed -i 's|</manifest>|<uses-permission android:name="android.permission.INTERNET"/><uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/><uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/><uses-permission android:name="android.permission.WAKE_LOCK"/><uses-permission android:name="android.permission.FOREGROUND_SERVICE"/><uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/><uses-permission android:name="android.permission.POST_NOTIFICATIONS"/></manifest>|' android/app/src/main/AndroidManifest.xml
          sed -i 's|</application>|<receiver android:enabled="true" android:exported="true" android:name=".BootReceiver" android:permission="android.permission.RECEIVE_BOOT_COMPLETED"><intent-filter><action android:name="android.intent.action.BOOT_COMPLETED"/><action android:name="android.intent.action.QUICKBOOT_POWERON"/></intent-filter></receiver></application>|' android/app/src/main/AndroidManifest.xml

      - name: Create BootReceiver with imports
        run: |
          mkdir -p android/app/src/main/java/com/anxroall/collector
          cat > android/app/src/main/java/com/anxroall/collector/BootReceiver.java << 'EOF'
          package com.anxroall.collector;
          import android.content.BroadcastReceiver;
          import android.content.Context;
          import android.content.Intent;
          import com.getcapacitor.BridgeActivity;
          
          public class BootReceiver extends BroadcastReceiver {
            @Override
            public void onReceive(Context context, Intent intent) {
              if (Intent.ACTION_BOOT_COMPLETED.equals(intent.getAction()) || 
                  Intent.ACTION_QUICKBOOT_POWERON.equals(intent.getAction())) {
                Intent i = new Intent(context, BridgeActivity.class);
                i.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                context.startActivity(i);
              }
            }
          }
          EOF

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TG-Collector-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
